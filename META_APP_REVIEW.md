# Meta App Review

The canonical, production-specific submission packet is
[`docs/meta-app-review-brief.md`](docs/meta-app-review-brief.md).

Do not copy older chat or `/tmp` drafts. They omit the insights permission,
describe an unsafe forwarded reviewer mailbox, and predate the fail-closed
capability registry.
